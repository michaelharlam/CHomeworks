#include <stdio.h>
#include <stdlib.h>

void swap(int *a, int *b) {
}

int halfQSort(int array[]) {
    int firstElement = array[0];

}

int main(void) {
    int array[100];
    for (int i = 0; i < 100; ++i) {
        array[i] = rand() % 100;
    }

}